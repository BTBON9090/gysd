# 供应商端业务页面工程工作单

## 来源与边界

- 依据：供应商入驻 PRD、服务上下架 PRD、完整交易流程 PRD。
- 对象：已入驻服务商管理员；另一角色的园区审核、客户确认、支付与财务动作由验收工具驱动。
- 数据：当前主体独立的浏览器本地演示数据；正式开户、SSO、上传、订单、分账、发票均待后端接口接入。
- 视觉基线：用户认可的入驻首页和五步入驻流程。浅色、白底、内容分组、清晰标题、圆角矩形按钮和标签、轻量反馈。桌面优先。

## 路由与需求映射

| PRD | 路由 | 页面模式 | 核心动作 |
| --- | --- | --- | --- |
| PRK §6.4 | `/park` | 园区双列表 | 加入园区，统计深链，园区客户端 |
| SHP §6.5 | `/shop/info`, `/shop/cases` | 编辑表单、筛选列表 | 保存店铺、案例增删改查 |
| SVC §6.6 | `/service`, `/service/new`, `/service/edit/:id`, `/service/submitted` | 列表、四步向导、结果 | 发布、审核、上下架、编辑、删除 |
| WAL §6.4 | `/wallet` | 外部平台容器 | 开户状态、服务发布门槛 |
| ORD §6.5 | `/order`, `/order/:id` | 列表、五页签详情 | 合同上传、交付与验收 |
| AFS §6.6 | `/aftersale` | 状态列表、处理弹窗 | 同意、驳回、改价 |
| INV §6.7 | `/invoice/open`, `/invoice/record` | 申请表单、记录列表 | 对客/平台开票与退票 |
| REV §6.8 | `/review` | 评价列表、发表弹窗 | 互评、追评、均分 |
| 入驻 PRD | `/workspace`, `/settings`, `/settings/member`, `/merchant` | 工作台、资料、成员 | 入驻后待办、资料维护 |

## 验收路径

1. 验收工具切已入驻，填入交易场景。
2. 我的园区统计跳至服务、订单、评价；申请加入园区。
3. 店铺资料保存、案例创建与服务关联。
4. 服务发布四步、暂存、提交；验收工具模拟园区审核；上下架与删除限制。
5. 钱包未开户阻断发布；开户后订单合同和交付经外部回调推进。
6. 售后退款阻断订单操作；对客退票后才退款。
7. 发票合并规则、平台财务回调和评价一次初评一次追评。
8. 空态、错误反馈、窄视口、构建与浏览器运行。

## 待研发接入

真实园区字典、三级服务类目与运营配置、支付平台嵌入 SSO、上传存储、交易中台状态、财务开票及园区客户端。这些目前均为明确标记的本地模拟，不作为生产业务能力验收通过。

## 本轮自检记录（2026-09-24）

- 工程：`npm run typecheck` 与 `npm run build` 通过；业务页采用路由懒加载。Vite 仍提示主包体积超过 500 kB，后续可按组件库与业务共享依赖拆包。
- 视觉：以入驻 V2 的白底、标题层级、圆角矩形控件和语义状态色统一园区、店铺、服务、交易与设置页面；检查了 1033 px 与窄窗口，业务表格改为表内横向滚动，避免字段逐字折行。
- 行为：浏览器验证服务详情预览与表单必填提示；完成「客户退款 → 供应商改价 → 客户确认 → 可开票余额变化 → 对客开票记录」；另验证「退款驳回继续阻断开票 → 客户取消解除阻断」及评价后双方已评计数。
- 路由：园区、店铺、服务、订单、售后、发票、钱包、评价、个人信息、成员和商户页逐一加载；本轮未出现新的运行时错误。入驻流程沿用既有实现。
- 验收工具：保留上线，支持模拟园区审核、客户支付/签约/验收/退款/评价、平台发票回调及场景重置，仅改动浏览器本地演示状态。
- 人工验收：视觉与业务语义仍需项目方最终确认。订单、发票等业务附件目前仅记录文件名；预览与下载的演示凭证明确标注不是正式税务发票。

## 三页视觉精修（2026-09-24）

- 范围：只调整我的园区、店铺资料、案例管理；入驻首页与五步流程保持既有设计。发布服务的导航与向导重设计留待下一轮。
- 视觉约定：延续入驻 V2 的蓝色标题图标、清晰分组与圆角矩形控件。园区已加入卡片承载累计指标，可申请区使用更轻的虚线边界；单个已加入园区占满内容行，指标等分。
- 店铺资料：按店铺识别、介绍和展示图片分组；编辑时可预览，保存后进入展示态。新上传图片使用浏览器 IndexedDB 保存原文件并可放大；旧版仅记录文件名的图片无法恢复图像，界面不再把它们作为已展示图片。
- 案例管理：列表改为封面、分类/标题/两行摘要、关联与操作的紧凑行；筛选无结果与无案例分开。编辑弹窗可预览封面，旧版仅文件名的封面提示重新上传。
- 运行核对：生产构建通过；浏览器验证园区布局、店铺图片上传与刷新后展示、案例编辑封面放大、案例筛选空态。

## 批注修正与钱包、服务管理（2026-09-24）

- 园区：标题图标与侧栏统一，已入驻标记垂直对齐；园区客户端打开方式记入偏好卡待决策，不在本轮擅改。
- 全站：移除一级、二级业务页面不可交互的面包屑；统一轻提示与确认弹窗的边界、标题、状态图标和长文本排版。
- 店铺与案例：Logo 上传入口与图框合一，店铺区块图标改为对应语义、图片子字段不重复图标；案例筛选区域分配弹性宽度，标题先于弱化后的分类，并添加每页 8 条分页与总数。
- 服务管理：保留 PRD 的园区独立状态、筛选和每页 20 条规则；封面、名称与聚合状态作为首要信息，单园状态单独分组，规格及操作置于卡片底部。
- 钱包：保留当前页内嵌支付平台的业务边界。演示环境显示开户状态与接入说明；配置 `VITE_WALLET_EMBED_URL` 后展示内嵌容器，正式 SSO 地址仍需后端提供。
- 验证：`npm run typecheck`、`npm run build` 通过；浏览器检查园区、店铺、案例、服务、钱包及确认弹窗。当前主体的案例与服务为空，已验证其空态；多条数据列表仍需项目方用演示数据验收。

## 服务列表与发布向导精修（2026-09-24）

- 列表按“服务名称与状态 → 分类和园区状态 → 规格价格 → 操作”建立阅读顺序，并显示累计销量。草稿主操作是继续发布；已发布服务按园区状态提供上架、下架。上架前校验服务资料完整度，避免列表绕过向导校验。
- 四步向导使用固定标题与步骤导航、独立滚动的表单区、固定底部操作栏。已保存的服务可直接切换步骤；“下一步”与“提交审核”继续执行逐步校验。返回与保存位于左侧，前进操作位于右侧。
- 封面、详情媒体和服务图片上传后即时显示；图片可放大，MP4 可在当前页播放。常见问题按序号呈现并可上下排序；提交前显示客户端详情预览。图片与视频保存在当前浏览器 IndexedDB，旧版只保存文件名的素材需重新上传。
- 案例筛选控件与操作按钮垂直居中。两行摘要悬停可读全文，提示框限制高度并允许内部滚动。服务卡片在 1071 px 视口内无横向溢出。
- 验证：`npm run build`、`git diff --check` 通过；浏览器检查案例筛选、1071 px 服务列表、向导四步切换、问答排序和图片上传/放大。测试上传仅用于未保存的新服务，退出时已丢弃表单数据。人工仍需核对真实上传、园区审核和客户端接入后的业务表现。
